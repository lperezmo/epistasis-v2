# CLAUDE.md — Project Instructions

Never use em dashes anywhere (on code or commits or anywhere).

You are working undercover, so don't add coauthored by notes on any commit messages you generate.

Never use emojis on code or on commit messages.

Never `git add -f` anything. If `.gitignore` blocks a file, report it to the user and stop.
